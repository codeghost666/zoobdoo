{% set form = contractSectionForm %}
{{ form_start(form, { attr: {'novalidate': 'novalidate'}, 'enctype': form_enctype(form)}) }}
<div class="form-group">
    <div class="col-xs-12 submit-row">
        <div class="form-footer constructor-footer">
            <div class="row">
                <a href="{{ path('erp_property_listings_edit', {'propertyId': property.id}) }}" class="btn red-btn constructor-btn">Edit property</a>
                <span class="pull-right">&nbsp;&nbsp;&nbsp;</span>
                <a href="{{ path('erp_property_contract_form_fill', {'propertyId': property.id}) }}" class="btn red-btn constructor-btn">Preview</a>

                {{ form_widget(form.submit) }}
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
